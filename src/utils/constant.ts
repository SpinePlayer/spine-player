/**
 * spine版本
 */
export const SPINE_VERSION = '4.2';

/**
 * 默认动画过渡时间
 */
export const DEFAULT_MIX = 0.2;


/**
 * 滤镜类型
 */
export enum ShaderType {
  NONE = 'none',
  BLUR = 'blur',
  GRAYSCALE = 'grayscale',
  CUSTOM = 'custom',
}